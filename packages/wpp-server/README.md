# wpp-server

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build wpp-server` to build the library.

## Running unit tests

Run `nx test wpp-server` to execute the unit tests via [Jest](https://jestjs.io).
